[Unit]
Description=SmartMoney Orchestrator
After=docker.service
Requires=docker.service

[Service]
ExecStart=/usr/bin/docker compose --env-file /opt/bot/.env.live up orchestrator
Restart=always
StartLimitBurst=3
WatchdogSec=40

[Install]
WantedBy=multi-user.target
